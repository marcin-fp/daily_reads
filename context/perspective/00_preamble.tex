% Style
\usepackage{mathpazo} % Palatino text & math
\linespread{1.05}
\usepackage{microtype}           % refined justification / kerning
\usepackage[top=1.10in,bottom=0.9in,left=1.05in,right=1.05in,headheight=19pt]{geometry}
\usepackage{xcolor}
\definecolor{accent}{HTML}{6B1F2E}
\definecolor{link}{HTML}{1C3F6E}
\usepackage{calc}
\usepackage{svg}

% Headings
\usepackage{titlesec}
\usepackage{needspace}

% Roman-numeral section numbers, centered small-caps heads (RevTeX hallmark)
\renewcommand{\thesection}{\Roman{section}}
\titleformat{\section}[block]
  {\needspace{3\baselineskip}\filcenter\normalfont\large\bfseries}
  {\thesection.}{0.6em}{}
\titlespacing{\section}{0pt}{3.9ex plus 0.6ex minus 0.2ex}{1.0ex plus 0.2ex}

% Run-in paragraph heads: bold, accent-colored (the document leans on these)
\titleformat{\paragraph}[runin]
  {\normalfont\bfseries}{}{0pt}{}
\titlespacing{\paragraph}{0pt}{1.1ex plus 0.4ex}{0.8em}

% Footnotes
\usepackage[hang,flushmargin,bottom,multiple]{footmisc}
\renewcommand{\footnotelayout}{\footnotesize}
\setlength{\footnotesep}{0.7\baselineskip}
\renewcommand{\footnoterule}{\noindent\textcolor{accent!55}{\rule{2.2in}{0.4pt}}\vspace{4pt}}
\let\oldfootnotesize\footnotesize % Back up original just in case
\renewcommand{\footnotesize}{\scriptsize}

% Headers
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\scshape\footnotesize\color{accent}(For internal circulation only)}
\fancyhead[R]{\footnotesize\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\headrule}{\hbox to\headwidth{\color{accent!60}\leaders\hrule height \headrulewidth\hfill}}
\renewcommand{\footrulewidth}{0pt}

% First page
\fancypagestyle{titlepg}{%
  \fancyhf{}\renewcommand{\headrulewidth}{0pt}%
  \fancyfoot[C]{\footnotesize\thepage}%
}

% Hyperlinks
\usepackage{xurl}
\usepackage[breaklinks]{hyperref}
\hypersetup{colorlinks=true, urlcolor=link, linkcolor=accent, citecolor=accent, breaklinks=true}
\urlstyle{same}
\usepackage{fnpct} % Fixes footmisc[multiple]
\floatingpenalty=0
\usepackage{etoolbox}

% Custom
\newcommand{\rulethin}[1][\textwidth]{\noindent\textcolor{accent}{\rule{#1}{0.4pt}}}
\newcommand{\rulethick}[1][\textwidth]{\noindent\textcolor{accent}{\rule{#1}{1.2pt}}}

% Section summary
\usepackage[most]{tcolorbox}
\definecolor{boxgray}{HTML}{ECECE8}
\newtcolorbox{summarybox}{%
  enhanced, breakable,
  colback=boxgray, colframe=accent,
  boxrule=0pt, leftrule=2.5pt,
  arc=2pt, outer arc=2pt,
  left=12pt, right=12pt, top=9pt, bottom=10pt,
  before skip=8pt, after skip=14pt,
  fontupper=\small,
  %before upper={\textcolor{accent}{\scshape\bfseries In brief.}\hspace{0.6em}}%
}
\usepackage{booktabs}
\usepackage{tabularx}
\tcbuselibrary{raster}
\usepackage{enumitem}

\newtcolorbox{datasignaltablebox}{
  colback=black!1,
  colframe=black!30,
  coltitle=black,
  title={Data, memory, and platform advantage},
  fonttitle=\bfseries,
  boxrule=0.4pt,
  arc=2mm,
  left=2mm,
  right=2mm,
  top=2mm,
  bottom=2mm
}
% Attention box
\usepackage{fontawesome5}
%\definecolor{highlightboxacc}{HTML}{B45309} % Muted Ochre / Mustard
%\definecolor{highlightbg}{HTML}{FFFBEB}  % Soft Alabaster Yellow
%\definecolor{highlightboxacc}{HTML}{D97706} % Rich Amber / Dark Gold
%\definecolor{highlightbg}{HTML}{FEF3C7}  % Warm Cream Yellow
\definecolor{highlightbg}{HTML}{F4F4F0} % Slightly different tint, or keep boxgray
\definecolor{highlightboxacc}{HTML}{D9534F} % Example: A vibrant alert/highlight color
\newtcolorbox{highlightbox}{%
  enhanced, breakable,
  colback=highlightbg, colframe=highlightboxacc,
  boxrule=0pt, leftrule=2.5pt,
  arc=2pt, outer arc=2pt,
  left=12pt, right=12pt, top=9pt, bottom=10pt,
  before skip=8pt, after skip=14pt,
  fontupper=\small,
  before upper={\textcolor{highlightboxacc}{\large\faExclamationTriangle}\hspace{0.8em}}%
% Or try: \faExclamationTriangle, \faFire, \faRocket, \faBomb
}

% Brief box
\newtcolorbox{briefbox}{%
  enhanced, breakable,
  colback=boxgray, colframe=accent,
  boxrule=0pt, leftrule=1.5pt, % Thinner line
  arc=0pt, outer arc=2pt,
  left=8pt, right=8pt, top=4pt, bottom=4pt, % Tighter padding
  before skip=6pt, after skip=10pt,
  fontupper=\small\itshape, % Italicized text helps distinguish it as a brief
}

% Figure
\usepackage[font={small,it}]{caption}

\interfootnotelinepenalty=0


\newtcolorbox{abstractbox}{
    colback=white,
    colframe=black!35,
    arc=0mm,
    boxrule=0.5pt,
    left=15pt,
    right=15pt,
    top=12pt,
    bottom=12pt,
    fonttitle=\bfseries\large,
    coltitle=black,
    detach title,
    before upper={\noindent}
}